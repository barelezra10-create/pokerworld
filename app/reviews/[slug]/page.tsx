import {reviews} from '@/lib/reviews';
import {notFound} from 'next/navigation';
export function generateStaticParams(){return reviews.map(r=>({slug:r.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const r=reviews.find(r=>r.slug===slug);return {title:r?`${r.name} Review: Features & Fit | Pokerhub`:'Review not found',alternates:{canonical:'/reviews/'+slug}}}
export default async function Review({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const r=reviews.find(r=>r.slug===slug);if(!r)notFound();return <main className="review-article"><a className="back-link" href="/reviews">← All training reviews</a><p className="eyebrow">{r.category} · UPDATED 7 SEPTEMBER 2026</p><h1>{r.name} review</h1><p className="lead">{r.summary}</p><div className="disclosure-note">Editorial research based on official documentation. We have not conducted a hands-on product test. No affiliate relationship or tracking link is active for this review.</div><section><h2>Who it may suit</h2><p>{r.verdict}</p></section><section><h2>What the official product describes</h2><ul>{r.features.map(f=><li key={f}>{f}</li>)}</ul></section><section><h2>What to check first</h2><p>{r.limits}</p><p>We do not quote pricing here because plans and promotions change. Confirm the full price, billing interval, included features and refund terms on the official product website.</p></section><section><h2>Our recommendation</h2><p>{r.fit}. Compare the product with your actual study routine, and try available sample content before paying. No study tool guarantees winning results.</p><a className="primary" href={r.url} target="_blank" rel="noopener noreferrer">Read official product information ↗</a></section><p className="fine">Source: official product documentation linked above, checked 7 September 2026. <a href="/affiliate-disclosure">Read our disclosure.</a></p><a href="/play" className="back-link">Try the free Pokerhub table →</a></main>}
