import {guides} from '@/lib/guides';
import {notFound} from 'next/navigation';
export function generateStaticParams(){return guides.map(g=>({slug:g.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const g=guides.find(g=>g.slug===slug);return {title:g?g.title+' | Pokerhub':'Guide not found',description:g?.summary,alternates:{canonical:'/learn/'+slug}}}
export default async function Guide({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const g=guides.find(g=>g.slug===slug);if(!g)notFound();return <main className="learning-article"><a className="back-link" href="/learn">← Learning library</a><p className="eyebrow">{g.category} · 7 SEPTEMBER 2026</p><h1>{g.title}</h1><p className="lead">{g.summary}</p>{g.sections.map(s=><section key={s.title}><h2>{s.title}</h2>{s.paragraphs.map(p=><p key={p}>{p}</p>)}</section>)}<aside className="practice-note"><h2>Try it without stakes</h2><p>{g.exercise}</p>{g.category!=='Casino explained'&&<a href="/play">Open the free practice table →</a>}</aside>{g.sources&&<section><h2>Reference</h2>{g.sources.map(s=><p key={s.url}><a href={s.url} target="_blank" rel="noopener noreferrer">{s.title} ↗</a></p>)}<p className="fine">References checked 7 September 2026. Examples and exercises are Pokerhub editorial material.</p></section>}<section><h2>Continue learning</h2><div className="related-guides">{g.related.map(slug=>{const r=guides.find(g=>g.slug===slug)!;return <a key={slug} href={'/learn/'+slug}>{r.title} →</a>})}</div></section><p className="fine">Educational content, not a promise of winning results. <a href="/affiliate-disclosure">How our content and recommendations work.</a></p></main>}
